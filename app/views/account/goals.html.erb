<main>
  <div class="content-wrapper">
    <h1>My goals"<%= @search_term %>"</h1>
  </div>
  <div class="content-wrapper">
    <% if(@goals.any?) %>
      <ul class="bare-list float-grid css-grid">
      <% @goals.each do |idea|%>
        <li>
    
        </li>
      <% end %>
      </ul>
    </div>
    <div class="see-more">
      <button class="btn" type="button" data-more="true">See more</button>
    </div>
    <% else %>
      <h3>You currently don't have any goals.</h3>
    <% end %>
</main>
